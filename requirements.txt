# LangChain 1.0 and LangGraph
langchain>=1.0.0
langchain-core>=1.0.0
langchain-openai>=1.0.0  # Used for OpenRouter (via ChatOpenAI with custom base_url)
langgraph>=1.0.0


# MCP (Model Context Protocol)
mcp>=1.0.0

# Observability
langsmith>=0.5.0

# API clients
requests>=2.31.0
httpx>=0.27.0
beautifulsoup4>=4.12.0  # For extracting text from web pages

# Environment and utilities
python-dotenv>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Date handling
python-dateutil>=2.8.2

# CLI and user interaction
rich>=13.0.0
prompt-toolkit>=3.0.0

# Document generation
python-docx>=1.1.0
Pillow>=10.0.0  # For image handling in documents

# Geographic distance calculation
geopy>=2.4.0

scikit-learn==1.7.2
k-means-constrained>=0.8.0  # For min/max cluster size constraints
numpy==2.3.5
geopandas==1.1.1
contextily==1.7.0
shapely==2.1.2
matplotlib==3.10.7
adjustText>=0.8  # For non-overlapping map labels
